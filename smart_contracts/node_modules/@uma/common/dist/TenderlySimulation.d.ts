import { Options as RetryOptions } from "async-retry";
interface ForkParams {
    id: string;
    root?: string;
}
export interface TenderlySimulationParams {
    chainId: number;
    to: string;
    input?: string;
    value?: string;
    from?: string;
    timestampOverride?: number;
    fork?: ForkParams;
}
interface ResultUrl {
    url: string;
    public: boolean;
}
export interface TenderlySimulationResult {
    id: string;
    status: boolean;
    resultUrl: ResultUrl;
}
export declare const simulateTenderlyTx: (simulationParams: TenderlySimulationParams, retryOptions?: RetryOptions) => Promise<TenderlySimulationResult>;
export {};
