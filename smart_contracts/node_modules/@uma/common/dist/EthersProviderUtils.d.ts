import { ethers } from "ethers";
declare class EthersRetryProvider extends ethers.providers.StaticJsonRpcProvider {
    readonly retries: number;
    readonly delay: number;
    readonly providers: ethers.providers.StaticJsonRpcProvider[];
    constructor(params: ConstructorParameters<typeof ethers.providers.StaticJsonRpcProvider>[], chainId: number, retries: number, delay: number);
    send(method: string, params: Array<any>): Promise<any>;
    _trySend(provider: ethers.providers.StaticJsonRpcProvider, method: string, params: Array<any>): Promise<any>;
}
export declare function getRetryProvider(chainId: number): EthersRetryProvider;
export {};
